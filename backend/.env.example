# MongoDB Connection String
# The backend reads `process.env.MONGO_URI` at runtime. Add this variable
# in your deployment provider (Vercel) as `MONGO_URI`.
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/intervue-poll

# (Optional) some platforms also accept `MONGODB_URI`; keep for reference
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/intervue-poll

# Server Port (optional, defaults to 4000)
PORT=4000

# Frontend allowed origins (comma-separated)
# FRONTEND_ORIGIN=http://localhost:5173

# Node Environment
NODE_ENV=development
